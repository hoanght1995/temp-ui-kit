export const isWeb = true;

export const isIos = false; // for checking only for iOS devices

export const isAndroid = false; // for checking only for Android devices

export const isNativeApp = false; // for checking the native app from folder mobile on remitano repo
